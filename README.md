# 💸 xpense-cli

A lightweight command-line **expense tracker** written in Python.  
Track your spending, set budgets, and view summaries — all from your terminal.

---

## 🚀 Quick Start

```bash
# Clone the repo
git clone https://github.com/<YOUR_USERNAME>/xpense-cli.git
cd xpense-cli

# Run directly
python xpense.py --help
